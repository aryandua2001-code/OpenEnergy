node_modules/
dist/
.DS_Store
*.local

# Source video files — too large for GitHub (some exceed the 100MB limit)
# The website uses public/frames/ (JPG sequence), not these videos directly
public/videos/
videos/
